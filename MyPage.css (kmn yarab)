* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Poppins", sans-serif;
}

body {
  background: #050816;
  color: #f1f5f9;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background-image:
    radial-gradient(circle at 0% 0%, #ff6bcb15, transparent 60%),
    radial-gradient(circle at 100% 100%, #63e6ff15, transparent 60%);
}

.wrapper {
  width: 90%;
  max-width: 1100px;
  display: grid;
  grid-template-columns: 1.4fr 1fr;
  background: rgba(15, 23, 42, 0.7);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.07);
  border-radius: 26px;
  overflow: hidden;
  box-shadow: 0 25px 60px rgba(0,0,0,0.6);
}

.left-panel {
  padding: 3rem;
  border-right: 1px solid rgba(255,255,255,0.05);
}

.title {
  font-size: 2rem;
  font-weight: 600;
  margin-bottom: 1rem;
  line-height: 1.2;
}

.title span {
  font-size: 1.7rem;
}

.subtitle {
  margin-bottom: 2rem;
  color: #cbd5e1;
  line-height: 1.6;
}

.features .feat {
  margin-bottom: 0.7rem;
  font-size: 0.95rem;
  opacity: 0.9;
}

.right-panel {
  padding: 3rem;
  background: rgba(2, 6, 23, 0.45);
}

.input-group {
  margin-bottom: 1.3rem;
  display: flex;
  flex-direction: column;
}

.input-group label {
  margin-bottom: 0.4rem;
  font-size: 0.9rem;
  opacity: 0.85;
}

input, textarea {
  padding: 0.75rem;
  border-radius: 14px;
  background: rgba(15, 23, 42, 0.55);
  border: 1px solid rgba(255,255,255,0.1);
  color: #f8fafc;
  outline: none;
  transition: 0.2s ease;
}

input:focus, textarea:focus {
  border-color: #ec4899;
  background: rgba(255,255,255,0.06);
}

textarea {
  min-height: 110px;
  resize: none;
}

.submit-btn {
  width: 100%;
  padding: 0.9rem;
  margin-top: 1rem;
  border-radius: 999px;
  background: linear-gradient(130deg, #f97316, #ec4899, #22d3ee);
  border: none;
  cursor: pointer;
  font-size: 1rem;
  font-weight: 600;
  color: #0f172a;
  transition: 0.25s;
}

.submit-btn:hover {
  opacity: 0.9;
  transform: translateY(-2px);
}

.form-note {
  margin-top: 1.2rem;
  font-size: 0.8rem;
  text-align: center;
  color: #cbd5e1;
}

@media (max-width: 850px) {
  .wrapper {
    grid-template-columns: 1fr;
  }

  .left-panel {
    border-right: none;
    border-bottom: 1px solid rgba(255,255,255,0.05);
  }
}
